import { PaintCanvasDirective } from './paint-canvas.directive';

describe('PaintCanvasDirective', () => {
  it('should create an instance', () => {
    const directive = new PaintCanvasDirective();
    expect(directive).toBeTruthy();
  });
});
